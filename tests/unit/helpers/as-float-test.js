import { asFloat } from '../../../helpers/as-float';
import { module, test } from 'qunit';

module('Unit | Helper | as float');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = asFloat('42.3');
  assert.equal(result, 42.3);
});
